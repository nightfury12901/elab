// Example use: Print first question to console
if (typeof dsaQuestions !== "undefined") {
  console.log(dsaQuestions[0]);
} else {
  console.error("dsaQuestions is not defined. Make sure data.js is loaded first.");
}
